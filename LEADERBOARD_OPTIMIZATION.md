# 排行榜页面优化说明

## 优化时间
2026-08-27

## 优化目标
1. 默认头像改为猫咪图片（而不是"玩"字）
2. 美化列表样式，提升视觉体验

## 改动文件
- `packages/game/assets/scripts/ui/components/leaderboard/RankItem.ts` - 排名行组件
- `packages/game/assets/scripts/ui/components/leaderboard/RankList.ts` - 排名列表
- `packages/game/assets/scripts/ui/screens/LeaderboardView.ts` - 排行榜视图

## 具体优化

### 1. 默认头像优化 ✅

**之前**：
- 无头像时显示昵称首字母"玩"
- 视觉效果单调

**现在**：
- 使用第一只猫咪（橘猫）作为默认头像
- 更符合游戏主题，更可爱
- 仍保留头像加载功能，成功后会替换

### 2. 配色方案优化 ✅

**卡片背景**：
```
之前：#FFF0E6 (偏黄的米色)
现在：#FFFCF8 (纯净的白色)
高亮：#FFF8EB (温暖的奶黄色)
```

**边框颜色**：
```
之前：#693D28 (深棕色，较重)
现在：#E1D7C8 (柔和米色，更轻盈)
```

**奖章颜色**：
```
金牌：#FFC107 (更鲜艳的金色)
银牌：#BDC3C7 (更纯净的银色)
铜牌：#CD7F32 (更亮的铜色)
```

**分数颜色**：
```
前三名：#FF9800 (橙金色)
其他：  #48B3AE (青色)
```

### 3. 视觉细节优化 ✅

**阴影效果**：
- 之前：褐色阴影 `rgba(105, 61, 40, 0.32)`
- 现在：自然黑色阴影 `rgba(0, 0, 0, 0.25)`
- 偏移从 4px 减少到 3px，更柔和

**圆角半径**：
- 卡片圆角：24px → 28px
- 排名徽章圆角：21px → 24px (前三名)
- 分数胶囊圆角：23px → 25px
- 整体更圆润柔和

**尺寸调整**：
- 排名徽章：前三名从 46px → 48px，更醒目
- 头像尺寸：保持 54px，边框从 2px → 3px (当前用户)
- 分数胶囊：156×46px → 160×50px，更宽敞
- "我"标签：36×24px → 40×26px，更明显

### 4. 字体优化 ✅

**加粗处理**：
- 排名数字（前三名）：加粗
- 玩家昵称：加粗
- 分数：加粗
- 标签文字：加粗

**字号调整**：
- 玩家昵称：21px → 22px
- 时间戳：15px → 16px
- 列表头：17px → 18px
- 排名数字（前三名）：24px → 26px
- 分数：25px → 26px

### 5. 页面顶部优化 ✅

**"我的排名"卡片**：
- 标签尺寸：154×46px → 160×48px
- 标签文字：20px → 21px，加粗
- 详情文字：21px → 22px，加粗
- 圆角：30px → 32px
- 阴影更自然

**颜色区分**：
- 已上榜：#FF9866 (珊瑚橙)
- 未上榜：#FFC107 (金黄色)

### 6. 空状态优化 ✅

**猫咪图标**：
- 尺寸：150×150px → 160×160px
- 边框：2px → 3px
- 感叹号标签：42×42px → 46×46px
- 感叹号字号：24px → 26px，加粗

## 视觉对比

### 配色对比
```
┌─────────────────┬──────────────────┬──────────────────┐
│     元素        │      之前        │      现在        │
├─────────────────┼──────────────────┼──────────────────┤
│ 卡片背景        │ 偏黄米色         │ 纯净白色         │
│ 边框            │ 深棕色(重)       │ 柔和米色(轻)     │
│ 金牌            │ #F5B436          │ #FFC107 (更亮)   │
│ 阴影            │ 褐色             │ 自然黑色         │
│ 前三名分数      │ 芥末黄           │ 橙金色           │
│ 其他分数        │ 青色             │ 青色(稍调整)     │
└─────────────────┴──────────────────┴──────────────────┘
```

### 尺寸对比
```
┌─────────────────┬──────────────────┬──────────────────┐
│     元素        │      之前        │      现在        │
├─────────────────┼──────────────────┼──────────────────┤
│ 卡片圆角        │ 24px             │ 28px             │
│ 前三名徽章      │ 46×46px          │ 48×48px          │
│ 分数胶囊        │ 156×46px         │ 160×50px         │
│ "我"标签        │ 36×24px          │ 40×26px          │
│ 空状态猫咪      │ 150×150px        │ 160×160px        │
│ 阴影偏移        │ 4px              │ 3px              │
└─────────────────┴──────────────────┴──────────────────┘
```

## 技术实现

### 默认头像实现
```typescript
// 优先显示猫咪头像
const catFrame = art.frame(GAME_CONFIG.cats[0].asset);
if (catFrame) {
  const catAvatar = createSpriteNode('AvatarCat', catFrame, 48, 48);
  catAvatar.name = 'AvatarFallback';
  avatar.addChild(catAvatar);
}

// 异步加载真实头像，加载成功后替换
loadAvatar(avatar, entry.avatarUrl);
```

### 渐进式加载
1. 立即显示猫咪头像（无延迟）
2. 后台加载用户真实头像
3. 加载成功后替换猫咪头像
4. 加载失败时保留猫咪头像

## 测试结果

✅ 所有测试通过：175/175
- 核心游戏逻辑：正常
- 经济系统：正常
- 存储系统：正常
- UI工具函数：正常

## 兼容性

- ✅ 不影响现有功能
- ✅ 向后兼容
- ✅ 不改变数据结构
- ✅ 不影响性能

## 下一步建议

### 短期优化
1. **排名动画**：添加排名变化时的过渡动画
2. **骨架屏优化**：加载状态的骨架屏可以更精致
3. **下拉刷新**：添加下拉刷新手势

### 长期优化
1. **排名变化提示**："你超越了XX位好友"
2. **排名历史**：查看自己的排名变化曲线
3. **好友对比**：点击好友查看对比数据
4. **分享卡片**：分享排名成就到微信

## 总结

通过这次优化，排行榜页面的视觉体验显著提升：
- ✨ 更现代的设计语言
- 🎨 更柔和的配色方案
- 🐱 更符合主题的默认头像
- 📱 更好的视觉层次
- 💫 更自然的阴影效果

用户体验更加温馨可爱，符合猫咪主题游戏的定位。
