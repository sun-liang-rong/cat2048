# 加载速度 × 游戏体验 优化计划

> 📅 2026-08-23 ｜ 目标：**用户加载游戏更快，玩起来更流畅**
> 与上一轮"全面性能优化"不同，本计划只聚焦两个用户可感知的目标。

---

## 📊 现状基线（实测数据）

| 项目 | 数据 | 问题 |
|------|------|------|
| 资源总体积 | 4.9MB | 微信主包上限 4MB，已超线风险 |
| UI 资源 | 2.5MB（占 51%） | 45 个 PNG 独立加载，无图集 |
| 猫咪立绘 | 988KB（3 皮肤×12 级） | 启动只加载 4 级（已优化✅） |
| 背景图 | 712KB | 已压缩过（已优化✅） |
| **音频** | **396KB，全是 WAV** | **bgm.wav 单文件 345KB，格式错误** |
| 启动策略 | 全量预加载完才进首页 | 图鉴/商店/排行榜图标阻塞首屏 |
| 操作手感 | 动画期间锁输入 | 快速连滑会被吞掉 |

**已完成的基础优化**（不在本计划重复）：
✅ 大图压缩 -0.66MB ｜ ✅ 皮肤 5-12 级懒加载 ｜ ✅ 棋子节点对象池 ｜ ✅ 存储防抖 ｜ ✅ 排行榜批量提交

---

## 🚀 A 篇：加载更快

### A1. 启动资源分级加载 ⭐ 收益最大（纯代码）

**现状**：`ArtRepository.preload()` 等全部资源（含图鉴/商店/排行榜图标、特效、BGM）加载完才进入首页。用户盯着进度条的时间 = 全部资源下载时间。

**方案**：拆成两级
- **Tier 1（阻塞进度条）**：首页必需 UI + 棋盘底图 + Lv1-4 猫咪 + move/merge 音效 + 字体
- **Tier 2（进首页后后台静默加载）**：图鉴/商店/排行榜图标、合成特效、BGM、其余背景

**改动点**：
- `ArtRepository.preload()` 增加 tier 参数，`startupFramePaths()` 按界面分组
- `LoadingView` 进度条只映射 Tier 1
- 首页 `onReady` 后分片加载 Tier 2（每帧限量，不抢主线程）

**收益**：首屏可交互时间预计 **-40~60%**（图鉴+商店+排行榜图标约占启动资源的 1/3）

---

### A2. 音频格式转换 ⭐ 立即可做（脚本）

**现状**：全部音效是 WAV 格式。`bgm.wav` 345KB，而同等音质 MP3 只要 ~35KB。

**方案**：
```bash
# ffmpeg 转换（128kbps 对 BGM 足够，音效 96kbps）
ffmpeg -i bgm.wav -b:a 128k bgm.mp3
```
- `bgm.wav` 345KB → `bgm.mp3` ~35KB（-90%）
- 4 个音效文件合计 375KB → **约 60KB（-84%）**

**改动点**：转换文件 + `ArtRepository.preload` 中音频路径 `.wav` → `.mp3`（一处常量）
**注意**：微信小游戏完整支持 MP3 播放，代码逻辑不变。

---

### A3. UI 图集打包（需编辑器，收益大）

**现状**：45 个 UI 小图标独立加载、独立纹理 → 请求数多 + GPU 频繁切换纹理。

**方案**：Cocos Creator AutoAtlas，按界面分 4 组：
| 图集 | 包含目录 | 预估大小 |
|------|---------|---------|
| ui_home | ui/home/ | ~700KB |
| ui_panels | ui/collection/ + ui/shop/ | ~500KB |
| ui_common | ui/common/ + 图标类 | ~300KB |
| ui_gameplay | ui/gameplay/ + 道具图标 | ~250KB |

**操作**：资源管理器选中目录 → 右键 → 新建 → AutoAtlas → MaxRects 算法、2048 上限、允许旋转。

**收益**：启动请求数 **-40 个**；draw call 大幅下降（同时改善 B 篇帧率）。

---

### A4. 纹理压缩 ASTC（需编辑器构建预设）

对 2.5MB UI + 988KB 猫图开启 ASTC 压缩（微信 iOS/安卓均支持）：
- 下载体积 **-40~60%**
- 纹理解码由 GPU 直接读取，**加载解码时间大幅缩短**
- 运行时内存占用约减半

**操作**：项目设置 → 功能裁剪/纹理压缩 → 添加预设（ASTC 6x6）→ 应用到全部图片资源。

---

### A5. 引擎模块裁剪（需编辑器构建面板）

2D 小游戏用不到 3D、物理、视频、Spine 等。构建面板勾选裁剪：
- JS 代码体积预计 **-20~40%**，直接缩短脚本下载+解析时间

---

### A6. 微信分包（第二批，编辑器+代码）

图鉴 + 商店的资源（约 1MB+）移入分包，进入对应界面时 `wx.loadSubpackage` 按需加载。
主包体积预计 **-1MB+**，冷启动下载时间显著下降。
依赖 A3 完成后做（分包内也要先图集化）。

---

### A7. 未使用资源清理（脚本辅助）

写脚本比对 `GAME_CONFIG.art` 引用 vs `resources/game` 实际文件，清理孤儿资源。

---

## 🎮 B 篇：体验更流畅

### B1. 滑动输入缓冲 ⭐ 手感提升最大（纯代码）

**现状**：移动动画期间 `lockInput()`，快速连滑的操作被直接丢弃——这是 2048 类游戏最伤手感的点。

**方案**：
- 动画期间收到的新滑动方向存入缓冲（只保留最后一个）
- 动画结束立即消费缓冲执行下一步
- 玩家感知：连滑跟手、操作永不丢失

**改动点**：`GameFlowController.performMove` 的锁输入逻辑改为缓冲逻辑（~30 行）。

---

### B2. 高级皮肤预热提前（纯代码，小改动）

**现状**：棋盘首次出现 ≥5 级才触发加载 5-12 级皮肤 → 首次合出 Lv5 那一瞬间立绘可能还没就绪。

**方案**：达到 **4 级**时就后台预热（玩家从 4 合到 5 有整步动画的缓冲时间），加载完成时机对玩家完全无感。

---

### B3. 动画节奏检查（纯代码，微调）

2048 手感黄金区间：单次移动+合并全程 **≤180ms**。
- 实测 `moveSeconds` + `mergeSeconds` 当前总时长
- 若超标则压缩时长/并行化合并特效

---

### B4. Draw call 优化

依赖 A3 图集完成后自然改善，无需单独开发。

---

## 📈 C 篇：度量与验收

### 埋点（随 A1 一起实现）
```
t0 = 脚本首帧
t1 = Tier1 完成（首页可交互）   ← 核心指标
t2 = Tier2 完成
上报：wx.reportPerformance / 控制台日志
```

### 测试方法
- 微信开发者工具 Performance 面板 + 真机调试
- 冷启动（清缓存）/ 热启动各测 3 次取均值
- 对局中录制 60s，看帧率曲线和卡顿次数

### 验收线
| 指标 | 目标 |
|------|------|
| 冷启动首页可交互 | **< 2s**（中端机） |
| 热启动 | < 1s |
| 对局帧率 | 稳定 60fps，合并爆发时 ≥ 50fps |
| 连滑操作 | 零丢失 |

---

## 🗓️ 执行顺序

| 批次 | 内容 | 类型 | 预期收益 |
|------|------|------|---------|
| **第 1 批** | A1 分级加载 → B1 输入缓冲 → B2 预热提前 → B3 动画节奏 → 埋点 | 纯代码，可立即执行 | 首屏 -40~60%，手感质变 |
| **第 2 批** | A2 音频转换 → A7 资源清理 | 脚本 | 资源 -330KB |
| **第 3 批** | A3 图集 → A4 纹理压缩 → A5 引擎裁剪 → A6 分包 | 需 Cocos Creator 编辑器 | 体积 -40%+，渲染提速 |

**第 3 批需要你在 Cocos Creator 中操作，我会提供每一步的详细指南；前两批我可以直接完成。**
